<?php

/**
 * Version 1.0.6.0.1 to 1.0.6.0.2 upgrade
 * 
 * Adds branch pickup selector block to flexitheme
 */

$helper = Mage::helper('flexitheme/setup');
/* @var $helper Epicor_FlexiTheme_Helper_Setup */
if ($helper->isModuleEnabled('Epicor_Comm')) {   
    if (!$helper->findLayoutBlock('epicor_comm/customer_branchpickup')) {
        $helper->createLayoutBlock('Branch Pickup Selector', 'epicor_comm/customer_branchpickup', 'epicor_comm/customer/branchpickup.phtml', NULL, NULL, NULL, NULL, 'epicor_comm.branchpickup', NULL);
    }
}
	